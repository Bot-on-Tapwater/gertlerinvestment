services:
  app:
    container_name: gertler
    image: node:22-alpine
    command: sh -c "npm install &&  npm run dev"
    ports:
      - 3050:3000
    working_dir: /frontend
    volumes:
      - ./:/frontend